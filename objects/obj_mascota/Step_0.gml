// Velocidad de la mascota
//var velocidad = 2; // Ajusta la velocidad según sea necesario

// Distancia mínima a la que la mascota se detendrá
//var distancia_minima = 16; // Ajusta la distancia según sea necesario

// Calcular la distancia entre la mascota y el personaje
//var distancia = point_distance(x, y, obj_personaje.x, obj_personaje.y);

// Si la distancia es mayor a la mínima, mover la mascota hacia el personaje
//if (distancia > distancia_minima) {
    // Calcular el ángulo hacia el personaje
  //  var direccion = point_direction(x, y, obj_personaje.x, obj_personaje.y);
    
    // Mover la mascota en esa dirección
    //x += lengthdir_x(velocidad, direccion);
    //y += lengthdir_y(velocidad, direccion);
//}
